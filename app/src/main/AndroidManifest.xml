<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fisecode.absentapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AbsentApp"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${apiKey}" />
        <activity android:name=".views.absentspot.AbsentSpotActivity"/>
        <activity android:name=".views.employeedetail.EditProfileActivity" />
        <activity android:name=".views.employeedetail.EmployeeDetailActivity" />
        <activity android:name=".views.leave.LeaveRequestActivity" />
        <activity android:name=".views.history.HistoryActivity" />
        <activity android:name=".views.changepassword.ChangePasswordActivity" />
        <activity android:name=".views.forgotpassword.ForgotPasswordActivity" />
        <activity android:name=".views.signin.SignInActivity" />
        <activity
            android:name=".views.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.main.MainActivity" />
    </application>

</manifest>